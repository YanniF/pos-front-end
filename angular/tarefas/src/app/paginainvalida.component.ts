import { Component } from '@angular/core';

@Component({
  selector: 'paginainvalida',
  template: `
    <h2> Página inválida</h2>
    <a routerLink="/tarefas">Voltar</a>
  `
})

export class PaginaInvalidaComponent{ }